# Plan — finish the sold loop (2026-09-08)

Single source for “what’s done / what’s next” after today’s work. Detail lives in `docs/handover.md`; wiring truth in `docs/wiring-checklist.md`.

## Done today (code on `main` or landing)

| When | What | PR / commits |
|---|---|---|
| Morning | Live X login + publish reliability (site URL, pool, Sign out, token refresh, fresh token on sign-in) | Direct `main` commits + related fixes |
| Morning | Changelog-style X post voice | `b41c502` |
| Morning | Projects page (delete test projects) | `18ae3e5` |
| Morning | Google Search Console verification file | #20 |
| Afternoon | Live Replies inbox (mentions, drafts, Post to X; keyword search optional via bearer) | #21 |
| Afternoon | Ship update pack (one note → post + article + screenshot + GEO) | #23 (auto-merge) |
| Afternoon | Owner set production env: OpenAI, Perplexity, Anthropic, QStash, etc. | Vercel dashboard |

## Remaining for “fully functional” (P0 only)

Do these in order. Skip P1/P2 until the smoke pass is green.

### 1. Smoke the live loop (no code)
1. Sign in on `https://xoopa.app`
2. Home → **Ship update** with a real short note
3. Confirm: Posts draft (+ screenshot if Blob is on), changelog URL, GEO score updates
4. Optional: Publish to X from the ship modal or Queue
5. Open **Replies** and confirm mentions load

### 2. Finish wiring gaps (verify in Vercel)
- [ ] Vercel Blob enabled (`BLOB_READ_WRITE_TOKEN`) — screenshots in Ship update
- [ ] `X_BEARER_TOKEN` if you want keyword feeds in Replies (mentions already work)
- [ ] Google Indexing service account if changelog should notify Google
- [ ] `CRON_SECRET` confirmed so ERI cron runs
- [ ] Dodo only if charging today

### 3. Code honesty still open
| ID | Work | Why |
|---|---|---|
| P0.5 | GEO: do not silently `simulateResponse` on missing/failed keys; label live vs simulated | Trust |
| P0.4 | Show “used your top posts” when generation uses reinforced embeddings | Moat visible |
| P0.6 | Landing copy: do not oversell video→X (still deferred) | Honesty |

### 4. Explicitly deferred (not today)
- Video→X (WebM → mp4 + chunked upload)
- Google AI Overviews
- P1 prompt library / gap→action / share of voice

## Definition of done tonight

A founder can: **sign in with X → Ship update → see post + article + (screenshot) + GEO check → optionally publish to X → open Replies** — without demo lies or empty stubs for those surfaces.
