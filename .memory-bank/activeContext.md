# Xoopa.app - Active Context

Phases 1–7 exist in code (foundation through diagnostics), plus billing. Marketing UI refreshed (Satoshi, soft grey/orange).

**Strategic north star:** best autonomous X + SEO + GEO loop for indie shippers — see `docs/handover.md` P0–P2.

**Current focus:** P0 — prove the live loop on production. Ship update pack (`POST /api/ship`) is in; Replies are live. Still want visible ERI reinforcement and GEO honesty when a provider call fails. Do not expand into research-suite or multi-network features until P0 exit criteria pass.
